# Third-party notices

Agent Teams includes open-source dependencies with their own licenses. Direct runtime dependencies include React, Electron Store, UUID and the Model Context Protocol client. The Electron distribution includes Electron, Chromium, Node.js and their associated notices.

The exact dependency versions are recorded in `package-lock.json`. Distributors must preserve license files bundled by Electron and verify transitive dependency notices for every released binary.
